@model IEnumerable<SistemaCobros.Models.Pago>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.monto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.mora)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_pago)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cartera.id_cartera)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Pago.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.monto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mora)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_pago)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cartera.id_cartera)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Pago.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_pago }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_pago }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_pago })
        </td>
    </tr>
}

</table>
